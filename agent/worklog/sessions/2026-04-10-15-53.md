# Session: 2026-04-10 15:53

## 完成的工作

### 開發伺服器啟動腳本

- 在 `run_server/start.sh` 建立一鍵啟動腳本，同時啟動 FastAPI（port 8000）與 Vite（port 5173），並在 Ctrl+C 時同時關閉兩個程序

### 前後端連線問題修復

- 修改 `backend/.env` 的 `DATABASE_URL`，將 hostname 從 `postgres`（Docker 內部）改為 `localhost`，讓本機直接執行 uvicorn 時也能連接到 Docker 中的 PostgreSQL

### Admin Dashboard 新功能實作

- 在 `frontend/src/types/index.ts` 補上 `is_hidden` 欄位至 `Event` interface，修正 `EventRow` 元件的 TypeScript 型別錯誤
- 修改 `frontend/src/pages/AdminDashboardPage.tsx` 的 `ManualAddEventButton`，從原本直接送出空 body（導致 422 錯誤）改為開啟 form modal，讓使用者填入活動名稱、開始時間、場地名稱、城市等必填欄位後才送出
- 在 `frontend/src/pages/AdminDashboardPage.tsx` 新增 `PasswordHashGenerator` 元件，使用 `bcryptjs`（cost=12）在瀏覽器端產生 bcrypt hash，供更新 `ADMIN_PASSWORD_HASH` 環境變數使用
- 在 `backend/app/routers/admin.py` 新增 `POST /admin/scrape/{provider}` endpoint，使用 FastAPI `BackgroundTasks` 在背景非同步觸發爬蟲，即時回傳 202 Accepted

### Commits 整理（Phase 1–8 補 commit）

- 將既有的 Phase 1–8 程式碼依 scope 拆分為 13 個 commits，每個 commit 控制在 500 行以內，確保 git history 可讀性

## 使用的 Skills

/commit

## Commits

| Hash | Message |
|------|---------|
| 734c923 | feat: init, and define skills and AI agent behave |
| 97c33d5 | chore(agent): add PM planning artifacts |
| 94c0997 | chore(agent): add planning and exec-plan artifacts |
| ab16305 | chore(back/db): project scaffold and Docker setup |
| 1c1baa1 | chore(front/layout): add frontend dependencies |
| ebe1de9 | feat(shared): define TypeScript types |
| a104104 | feat(back/db): add database models and migrations |
| a62b68a | feat(back/scraper): add scraper base, whitelist, and artist parser |
| b06ccb3 | feat(back/scraper): add FansiGo scraper, upsert runner, and entry point |
| 6a2eecb | feat(back/api): add REST API endpoints and admin auth |
| f9cb81f | feat(front/layout): add app shell and layout components |
| 994b891 | feat(front/layout): add event list and admin pages |
| c55e80d | feat(front/cards): add event cards, grid, filter bar, and hooks |
| 00ac751 | chore: add dev server startup script |

<!-- Last updated: 2026-04-10 15:53 -->
