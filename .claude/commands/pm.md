Execute the PM skill exactly as specified in @agent/skills/pm.md.

Arguments provided: $ARGUMENTS

If no arguments were provided, respond with:
⚠️  /pm 缺少必要參數。

正確語法：
  /pm @agent/artifacts/planning/{feature-name}.md

目前可用的 planning artifacts：
(list files in agent/artifacts/planning/)

請補上參數後重新執行。

Otherwise, read the referenced planning artifact and proceed with the skill steps.
